var Bounds = class Bounds {
  constructor(x, y, width, height) {
    this.x = x;
    this.y = y;
    this.width = width;
    this.height = height;
  }
};

function Log(msg) {
  log(`[Rounded My Window] ${msg}`);
}
