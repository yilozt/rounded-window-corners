// @ts-ignore

import { Global, BlurMode } from '@gi/Shell'
import { Effect }           from '@gi/Clutter'

declare const global: Global,
    log: any,
    logError: any,
    imports: any,
    _: (arg: string) => string
